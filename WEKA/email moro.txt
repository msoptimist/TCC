Olá, 

> você poderia me mandar as configurações utilizadas nos seus experimentos?  
Foi usado o package rminer do R, que utiliza o package kernlab, função ksvm, no caso, com a configuração em "automatic".

> foram criados modelos separados para cada um dos atributos de saída utilizando todos os atributos de entrada, correto? 
Sim.

Boa sorte para a sua investigação.

Cumprimentos.